name: Publish NuGet Package

# This workflow triggers when a release is published.
# It will only publish to NuGet.org if the release is NOT marked as a pre-release.
# 
# Workflow:
# 1. Create a draft release (via create-release workflow) - it may be marked as pre-release
# 2. Review the draft release
# 3. Uncheck "This is a pre-release" checkbox if you want to publish to NuGet
# 4. Click "Publish release" - this workflow will run and publish to NuGet.org
# 5. If you leave "This is a pre-release" checked, this workflow will skip publishing

on:
  release:
    types: [published]

jobs:
  publish:
    name: Publish to NuGet.org
    runs-on: ubuntu-latest
    # Only publish if this is NOT a pre-release (i.e., pre-release checkbox is unchecked)
    if: ${{ !github.event.release.prerelease }}
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Extract version from release tag
        id: version
        run: |
          # Extract version from tag (v1.2.3 or v1.2.3-alpha -> 1.2.3 or 1.2.3-alpha)
          VERSION="${{ github.event.release.tag_name }}"
          VERSION="${VERSION#v}"  # Remove 'v' prefix if present
          echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
          echo "Extracted version: ${VERSION}"

      - name: Update version in project file
        run: |
          PROJECT_FILE="Augustus/Augustus/Augustus.csproj"
          VERSION="${{ steps.version.outputs.VERSION }}"

          # Update the Version property in the csproj file
          sed -i "s/<Version>.*<\/Version>/<Version>${VERSION}<\/Version>/" "$PROJECT_FILE"

          # Verify the change
          grep -A0 "Version" "$PROJECT_FILE" | head -1

      - name: Restore dependencies
        run: dotnet restore Augustus/Augustus/Augustus.csproj

      - name: Build
        run: dotnet build Augustus/Augustus/Augustus.csproj --configuration Release --no-restore

      - name: Pack NuGet package
        run: dotnet pack Augustus/Augustus/Augustus.csproj --configuration Release --no-build --output ./nupkg

      - name: Publish to NuGet.org
        run: |
          NUPKG=$(find ./nupkg -name "*.nupkg" -type f | head -1)
          echo "Publishing package: $NUPKG"
          dotnet nuget push "$NUPKG" \
            --api-key ${{ secrets.NUGET_API_KEY }} \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate

      - name: Upload artifacts to release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ./nupkg/*.nupkg
            ./nupkg/*.snupkg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish release notes
        run: |
          echo "✅ Successfully published Augustus.AI version ${{ steps.version.outputs.VERSION }} to NuGet.org"
          echo "📦 Package: https://www.nuget.org/packages/Augustus.AI/${{ steps.version.outputs.VERSION }}"
